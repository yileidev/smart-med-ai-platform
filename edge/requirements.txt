# Jetson Orin Nano Super / Linux - Python依赖
# 跨平台兼容：Jetson/Linux/Windows

# MQTT通信
paho-mqtt>=1.6.1

# 传感器驱动 (仅Linux/Jetson)
# Linux上安装: pip install smbus2 w1thermsensor RPi.GPIO
# Windows上跳过，使用模拟模式
smbus2>=0.4.2; sys_platform == 'linux'
w1thermsensor>=2.0.0; sys_platform == 'linux'
# RPi.GPIO和Adafruit-Blinka仅在树莓派/Jetson上手动安装

# 音频处理
pyaudio>=0.2.13
sounddevice>=0.4.6       # 跨平台音频库

# 本地语音识别 (Vosk - 完全离线)
vosk>=0.3.45

# 数据处理
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# AI模型训练和推理
# PyTorch: Linux/Jetson使用CUDA版，Windows使用CPU版
# Jetson: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# Windows: pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.30.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# 工具库
python-dateutil>=2.8.0
pytz>=2023.3
colorlog>=6.7.0
tqdm>=4.65.0
pillow>=10.0.0
requests>=2.28.0

# 开发工具 (可选)
# pytest>=7.4.0
# black>=23.7.0
